<%= simple_form_for(list, defaults: { required: false }) do |f| %>
  <div class="form-inputs">
      <%= f.input :name %>
      <%= f.input :photos, as: :file, input_html: { multiple: true } %>
    </div>

    <div class="form-actions">
      <%= f.button :submit %>
    </div>
<% end %>
