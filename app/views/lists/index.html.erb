<div class="container">
  <h1>Lists#index</h1>

  <%= cl_image_tag("u6sf3s1w48ao46bi3grj",
      width: 400, height: 300, crop: :fill) %>

  <!-- for face detection -->
  <%= cl_image_tag("smm7ffpy1dbosbbd6sjx",
      width: 150, height: 150, crop: :thumb, gravity: :face) %>
  <hr>
  <div id="lists">
    <% @lists.each do |list| %>
      <p><%= link_to list.name, list %> <%= list.id %></p>
    <% end %>
    <hr>
  </div>

  <%= link_to "Create New list", new_list_path %>
</div>
