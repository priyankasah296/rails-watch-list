<%= simple_form_for(bookmark, defaults: { required: false }) do |f| %>
  <div class="form-inputs">
    <%= f.input :comment %>
    <%= f.input :movie_id %>
    <%= f.input :list_id %>
  </div>

  <div class="btn-primary">
    <%= f.button :submit %>
  </div>
<% end%>
